@inherits ScissorHands.Theme.PostViewBase
@layout MainLayout

<section class="post">
    <ul>
        @foreach (var tag in Document?.Metadata.Tags ?? Array.Empty<string>())
        {
            <li><a href="tags/@Uri.EscapeDataString(tag.ToLowerInvariant())">@tag</a></li>
        }
    </ul>
    <article>
        @((MarkupString)Document?.Html!)
    </article>
</section>
