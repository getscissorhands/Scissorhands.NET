# Plugins in ScissorHands.NET

Plugin options are exposed as `IReadOnlyDictionary<string, object?>` and should
be treated as immutable configuration. Plugin manifest names are matched
case-insensitively and must be non-empty and unique.

For more details, visit the [Plugins](https://getscissorhands.app/docs/plugins/) page.
