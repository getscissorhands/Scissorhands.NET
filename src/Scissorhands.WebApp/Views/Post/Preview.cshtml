@using Scissorhands.Services
@using Scissorhands.WebApp.ViewModels.Post

@inject IThemeService Theme

@model PostViewViewModel
@{
    var partial = Theme.GetPost(Model.Theme);
}

@Html.Partial(partial, Model)