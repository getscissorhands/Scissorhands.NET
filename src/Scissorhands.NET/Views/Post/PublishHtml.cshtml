@using Scissorhands.Models.Posts
@using Scissorhands.Services

@inject IThemeService Theme

@model PublishedContent
@{
    var partial = Theme.GetPostPartialViewPath(Model.Theme);
}

@Html.Partial(partial, Model)